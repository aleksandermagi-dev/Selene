# Selene Workspace Continuation

Before changing this repository, read completely:

- `docs/journal/SELENE_ACTIVE_CONTINUATION_LEDGER.md`
- the files named in that ledger under **Required reading before editing**

Confirm the ledger against `git status --short --branch` and recent Git history.
The ledger is a recovery aid, not permission to overwrite newer user work.

After a meaningful implementation, verification, documentation, or decision
checkpoint, refresh the active continuation ledger before ending the task. Keep
it compact and operational. Record conclusions, evidence, remaining work,
boundaries, dirty files, and the exact resume point. Do not store hidden
chain-of-thought, secrets, raw private records, or unnecessary transcript text.

The dated `docs/journal/SELENE_WORK_JOURNAL.md` remains the historical record.
Move completed milestone history there and let the active ledger describe only
the current resumable edge.

Preserve all existing work. Do not revert or overwrite unrelated changes. Do
not include `docs/HACKATHON_CODEX_WORKFLOW_LOG.md` in a commit unless Aleks
explicitly requests it.

