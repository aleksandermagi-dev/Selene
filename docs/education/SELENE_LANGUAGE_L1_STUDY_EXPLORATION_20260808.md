# Selene Language L1 Study Exploration

Date: August 8, 2026

Status: connected for now through one bounded visible sentence-scene activity;
reopenable whenever new evidence no longer fits

## Ethical Scope

This was ordinary deliberate Study, not a performance test or broad voice
assessment. Only the first Language Foundation direction was opened:

> L1 · See the meaning roles inside a sentence

L2 and L3 were not opened. The initial session paused after readable
source-linked notes. It later resumed for one ordinary L1 activity after the
representation machinery was complete.

## Installed Flow

- installed Selene version: `0.1.1`;
- Learning Compass goal: `5`;
- Study session: `1`;
- final session state: `completed`;
- final Compass state: `connected_for_now`;
- open questions: `0`;
- open clarifications: `0`;
- Selene-authored completion reflection: present and visible;
- automatic performance grading: not used.

## L1 Connection Evidence

The bounded activity kept one supplied scene stable across three forms:

- statement: `The seedling grows near the window.`
- missing-role question: `Where does the seedling grow?`
- clear callback: `It grows near the window.`

The workbench showed participant, action, and place as separate visible roles.
It recorded `the seedling` as the antecedent of `it`, asked only for the place
role, added no unsupported participant or relationship, and exposed no hidden
chain-of-thought.

Selene then formed this visible reflection from the representation output
rather than receiving reflection prose from Aleks:

> I can keep one supported scene stable while arranging its visible meaning
> roles as a statement, asking a useful question about the missing place role,
> and referring back to the seedling as it without changing who the pronoun
> names.

The reflection route connected L1 for now, completed the session, and closed
the pondering thread as `integrated_for_now`. The state remains explicitly
reopenable. Memory candidates stayed at zero before and after.

## What Selene Noticed

The first readable note preserved this boundary:

> Conversation may omit an understood subject, and passive or multi-clause
> sentences may organize the same roles differently.

The second readable note preserved this boundary:

> One surface form can sometimes perform more than one conversational act, so
> context may be needed to identify the intended purpose.

Both notes were classified as `revisit` rather than manufactured uncertainty.
Metacognition reported that each fit the current Study question and did not
request clarification.

The bounded continuation added three more readable observations. The first
made the sentence-core counterexample concrete:

> A pile of familiar words does not become a clear sentence when their
> relationships remain unspecified.

The pronoun/reference concept then preserved this boundary:

> A pronoun can remain grammatically valid while referring to more than one
> plausible participant.

The question-role concept preserved a separate evidence boundary:

> Why and how may require explanatory evidence that is not available even when
> the event itself is known.

All three were treated as grounded observations. The last two were formed from
their specific approved concepts. A separate clarification-only invitation on
the question-role concept returned `study_clarification_signal_not_present`:
Selene did not invent confusion just because the interface asked whether any
was present.

## Adapter Observation and Repair

The first attempted note exposed the internal relationship label
`sentence_formation` in visible Study text. This was an adapter presentation
gap, not evidence that Selene was confused or that the lesson failed.

Exploration stopped immediately after that single observation. The Study
adapter was repaired to exclude internal one-token and underscore/hyphen
routing labels from visible relationship and principle notes. It now advances
to the next readable, source-linked idea instead of handing NLO machine-shaped
scaffolding.

The original diagnostic note remains preserved rather than silently rewritten.
The two subsequent installed-app notes demonstrate the corrected path.

The follow-up usability gap was then repaired. Selene's Notepad now exposes the
exact approved concept she is looking at, sends that concept identifier with
both note invitations, and labels existing notes with their source concept.
This keeps the choice visible and prevents the ordinary UI from silently
defaulting every invitation to the first concept in a multi-concept session.

The same visual pass found a shared-shell repaint bug rather than a Teaching-
specific color rule. Long pages stretched the sidebar to the full document
height, allowing its controls to scroll away and leaving only a lower gradient
strip visible. Tabs also inherited the previous page's scroll position. The
sidebar is now viewport-bounded with its own overflow, shared panels use opaque
surfaces, saved theme values apply before paint, and workspace changes begin at
the top. Headless visual checks covered the L1 concept picker, a deeply scrolled
Cocoon Teaching page, and a deeply scrolled Tendril page.

Focused verification after the repair: 71 language, Study, formation, and NLO
tests passed; Python compilation and `git diff --check` passed with only the
existing Windows line-ending warnings. The final installed package passed its
health, privacy, transfer-state, and boundary verifier.

## Boundaries Confirmed

- memory write: false;
- identity and personality change: false;
- governance or authority change: false;
- training and LoRA: false;
- hidden retention: false;
- no question or uncertainty was invented;
- explicit clarification invitation returned no signal and created no note;
- no broad conversational probe was used;
- no pass/fail grade was assigned.

## Next Study Direction

L1 is connected for now and does not need repeated testing. The next language
direction is L2: change form while tracking number, time, event shape, and
negation. Open it only as its own small Study activity; L1 should be reopened
only if later use exposes a genuine mismatch.
